global:
  scrape_interval: 2s
  scrape_timeout: 1s
  evaluation_interval: 2s

scrape_configs:
  - job_name: ob1-project-metric-state
    file_sd_configs:
      - files:
          - /etc/prometheus/targets/ob1-projects.json
        refresh_interval: 1s
