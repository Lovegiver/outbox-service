delivery:
  max_attempts: 3
  timeout_seconds: 5
  retry_delay_seconds: 30

worker:
  interval_seconds: 10

security:
  jwt:
    secret_key: "replace-with-long-random-secret"
    algorithm: "HS256"
    access_token_expire_minutes: 600